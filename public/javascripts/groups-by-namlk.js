const userId = document.getElementById('user-id').innerText;
const groupId = document.getElementById('group-id').innerText;

sessionStorage.setItem('userId', userId);
sessionStorage.setItem('groupId', groupId);
